export default function populateDropdown({}) {
    let contentVisibility: boolean = false;
}

/*
//In the return, something like this:
<select
    value={}
    id={"something unique"}

    onChange={
        (event)=>{
        const eventHTML
        onEmployeeChange(event,)
        }
    }
>
    {
        allThings.map(item => sakjlfjasdlkjfafunctiontomakeitan<option>());
    }
<option>

</select>
 */

/*

 */